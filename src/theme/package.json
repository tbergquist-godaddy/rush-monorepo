{
  "name": "@tbergq/theme",
  "private": false,
  "version": "0.0.1",
  "main": "dist/index",
  "devDependencies": {
    "@babel/cli": "^7.11.6",
    "@babel/core": "^7.11.6",
    "@adeira/babel-preset-adeira": "^1.0.0"
  },
  "dependencies": {
    "@babel/runtime": "^7.11.2"
  },
  "scripts": {
    "build:cjs": "babel src -d dist --ignore \"node_modules\"",
    "build:flow": "babel src -d dist --ignore \"node_modules\" --out-file-extension .js.flow",
    "build": "rushx build:cjs && rushx build:flow"
  }
}
